# Working notes, not part of the published page.
note.md

# Camera masters for the real-robot clips. The page serves the encoded H.264 .mp4
# beside them; the originals are large, and some are HEVC, which Chrome and Firefox
# will not decode. Nothing on the site links to them.
assets/real_demo/*.mov
assets/real_demo/masters/

__pycache__/
.DS_Store

# Local handoff notes for this copy of the site.
HANDOFF.md
